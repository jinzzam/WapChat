<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <script src="https://code.jquery.com/jquery-3.3.1.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/2.1.0/socket.io.dev.js"></script>
    <link rel='stylesheet' href='/stylesheets/style.css'/>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/kfonts2.css" rel="stylesheet">

</head>
<body>
<h1>WAP General chat</h1>
    <textarea id="chat-body" readonly rows="20px"></textarea><br>

    <!--닉네임 사용여부 기능-->
    <p><label>Choose Online_id: <br/></label>
        Using nickname:<input type="radio" name="onlineid" id="onlineid_nick" value="usenicksql" onclick=""
                              javascript:radio_check(usenickname) checked>
        Using name:<input type="radio" name="onlineid" id="onlineid_name" value="usenamesql">
    </p>
    <label>Type what you want to say: </label>
    <input type="text" id="mytext" placeholder="type anything">
    <input type="button" value="chat" onclick="clk()"><br>


    <div id="UploadBox">
        <span id="uploadArea">
        <label for="fileBox">Choose A File : </label><input type="file" id="fileBox"><br>
        <label for="nameBox">Name : </label><input type="text" id="nameBox"><br>
        <button type="button" id="uploadButton">Upload</button><br>
    </span>
    </div>
    <input type="button">

<script src="/js/bootstrap.min.js"></script>
</body>

<script>
    var socket = io.connect("http://localhost:4000");
    var roomId = 1;
    socket.emit('join:room', {roomId: roomId});

    function clk() {
        var message = $('#mytext').val();
        socket.emit('chatReqG', {
            msg: message,
            user_id: '<%= user_id %>',
            checked_id: $('#onlineid_nick').is(':checked')
        });
    }

    socket.on('chatRes', function (data) {
        console.log(data);
        //nickname을 사용해서 나타내줄건지 아니면 name을 사용할건지_표현
        if (data.user_id != undefined) {
            $('#chat-body').html($('#chat-body').html() + data.user_id + ':' + data.msg + '\n');
        } else {
            $('#chat-body').html($('#chat-body').html() + data.user_name + ':' + data.msg + '\n');
        }
    });

    $('#mytext').keydown(function (key) {
        var message = $('#mytext').val();
        if (key.keyCode == 13 && message != "") {
            socket.emit('chatReqG', {
                msg: message,
                user_id: '<%= user_id %>',
                checked_id: $('#onlineid_nick').is(':checked')
            });
            $('#mytext').val(null);
            $('#mytext').focus();
        }
    });

    //파일 서버로 받아와서 서버에 저장
    window.addEventListener("load", Ready);

    function Ready() {
        if (window.File && window.FileReader) {
            //jQuery는 addEventListener가 아니라 bind로 이벤트를 만든다
            $('#uploadButton').bind('click', StartUpload);
            $('#fileBox').bind('change', FileChosen);
        } else {
            $('#uploadButton').innerHTML = "지원하지 않는 브라우저입니다."
        }
    }

    var selectedFile;

    function FileChosen(event) {
        selectedFile = event.target.files[0];
        $('#nameBox').val(selectedFile.name);
        //jQuery 로 쓸 수 있는 함수 : val(), text(), html()
    }

    var fileReader;
    var Name;

    function StartUpload() {
        if ($('#fileBox').value != "") {
            fileReader = new FileReader();
            console.log(selectedFile.type);
            Name = $('#nameBox').val();
            var Content = "<span id='NameArea'>Uploading " + selectedFile.name + " as " + Name + "</span>";
            Content += "<span id='Uploaded'> - <span id='MB'>0</span>/" + (selectedFile.size / 1048576).toFixed(3) + "MB</span>";
            $('#uploadArea').html(Content);
            fileReader.onload = function (event) {
                if (!event) {
                    var data = fileReader.content;
                } else {
                    var data = event.target.result;
                }
                socket.emit('Upload', {Name: Name, Data: data});
            }
            socket.emit('Start', {Name: Name, Size: selectedFile.size});
        }
        else {
            alert("Please Select A File");
        }
    }

    socket.on('MoreData', function (data) {
        console.log(data.Percent);
        UpdateBar(data.Percent);
        var Place = data.Place * 524288;
        console.log(Place);
        var NewFile = "";

        if (selectedFile.webkitSlice)
            NewFile = selectedFile.webkitSlice(Place, Place + Math.min(524288, (selectedFile.size - Place)));
        else {
            NewFile = selectedFile.slice(Place, Place + Math.min(524288, (selectedFile.size - Place)));
            fileReader.readAsBinaryString(NewFile);
        }
    });

    function UpdateBar(percent) {
        $('#percent').innerHTML = (Math.round(percent * 100) / 100) + '%';
        var MBDone = Math.round(((percent / 100.0) * selectedFile.size) / 1048576);
        $('#MB').html(MBDone);
    }

    /* <!--대화명 사용_code 변경예정-->
    function usenickname() {

    function usename(){
        var printnick = $('#onlineid_name').val();
    }

    var printnick = $('#onlineid_nick').val();
    */

</script>
</html>
